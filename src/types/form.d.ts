export interface FormDataType {
  name: string;
  phone: string;
  email: string;
  subject: string;
  text: string;
}

export interface FormErrorType {
  name: boolean;
  phone: boolean;
  email: boolean;
  subject: boolean;
  text: boolean;
}
